#include <iostream>

using namespace std;

/**
 * @brief Relationship is based on relation between inheritance of classes
 * There are two types of inheritance:
 * 1. isA relation - the derived class inherits and modify/override the properities of base class
 * 
 * 2. hasA relation - the drived class can use the properties of base class through its objects
 * there is not scope of modification of base class properties in the derived class
 * the derived class only uses the properties of base class through its objects.
 * 
 */

int main()
{

    return 0;
}